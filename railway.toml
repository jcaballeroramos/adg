# Railway deployment — sitio estático Artefactos de Guerra
# Usamos nixpacks.toml para build (con venv), aquí solo health check + restart.

[build]
builder = "NIXPACKS"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
